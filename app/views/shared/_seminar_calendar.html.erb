<h2>Event Calendar</h2>

<%= month_calendar(events: @seminars) do |date, seminars| %>
    <%= date %>

    <% seminars.each do |seminar| %>
        <div>
          <p><%= seminar.title %></p>
        </div>
      <% end %>
  <% end %>